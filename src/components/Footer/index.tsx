import styles from "./Footer.module.scss";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>2024 - Desenvolvido por Natália</p>
    </footer>
  );
};

export default Footer;
